@using ExamCake.BL.DTOs.ChiefDTO
@model ICollection<GetChiefDTO>


<!-- Begin Page Content -->
<div class="container-fluid">
    <div class="card shadow mb-4">
        <div class="card-header d-flex justify-content-between align-items-center py-3">
            <h6 class="m-0 font-weight-bold text-primary">Chiefs</h6>
            <a class="btn btn-primary" style="width: 160px" asp-controller="Chief" asp-action="Create">Create</a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Title</th>
                            <th width="300px">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (GetChiefDTO item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.FullName</td>
                                <td class="d-flex justify-content-around">
                                    <a class="btn btn-primary" asp-controller="Chief" asp-action="Update" asp-route-id="@item.Id">Update</a>
                                    <a class="btn btn-danger" asp-controller="Chief" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- /.container-fluid -->